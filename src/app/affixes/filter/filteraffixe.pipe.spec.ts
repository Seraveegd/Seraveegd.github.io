import { FilteraffixePipe } from './filteraffixe.pipe';

describe('FilteraffixePipe', () => {
  it('create an instance', () => {
    const pipe = new FilteraffixePipe();
    expect(pipe).toBeTruthy();
  });
});
