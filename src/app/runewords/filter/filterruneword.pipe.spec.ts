import { FilterrunewordPipe } from './filterruneword.pipe';

describe('FilterrunewordPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterrunewordPipe();
    expect(pipe).toBeTruthy();
  });
});
