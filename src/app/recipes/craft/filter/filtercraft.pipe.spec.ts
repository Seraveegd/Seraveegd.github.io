import { FiltercraftPipe } from './filtercraft.pipe';

describe('FiltercraftPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercraftPipe();
    expect(pipe).toBeTruthy();
  });
});
