import { FilternormalarmorPipe } from './filternormalarmor.pipe';

describe('FilternormalarmorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilternormalarmorPipe();
    expect(pipe).toBeTruthy();
  });
});
