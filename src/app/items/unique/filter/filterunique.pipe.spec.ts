import { FilteruniquePipe } from './filterunique.pipe';

describe('FilteruniquePipe', () => {
  it('create an instance', () => {
    const pipe = new FilteruniquePipe();
    expect(pipe).toBeTruthy();
  });
});
